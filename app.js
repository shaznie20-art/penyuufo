const $=id=>document.getElementById(id);let tasks=JSON.parse(localStorage.getItem('penyuufo_tasks')||'[]');const save=()=>{localStorage.setItem('penyuufo_tasks',JSON.stringify(tasks));render()};function render(){let q=($('search')?.value||$('search2')?.value||'').toLowerCase(),f=$('filter')?.value||$('filter2')?.value||'all';let list=tasks.filter(t=>t.title.toLowerCase().includes(q)).filter(t=>f==='all'||f==='done'&&t.done||f==='active'&&!t.done);let html=list.map(t=>`<div class="task"><button class="check ${t.done?'done':''}" data-id="${t.id}">${t.done?'✓':''}</button><span class="task-title ${t.done?'done':''}">${escapeHtml(t.title)}</span><span class="priority">${t.priority}</span><button class="delete" data-del="${t.id}">✕</button></div>`).join('')||'<p class="muted">No tasks found. Add your first task ✨</p>';if($('taskList'))$('taskList').innerHTML=html;if($('taskList2'))$('taskList2').innerHTML=html;let total=tasks.length,done=tasks.filter(t=>t.done).length,p=total?Math.round(done/total*100):0;['total','aPending'].forEach(id=>{if($(id))$(id).textContent=id==='total'?total:total-done});if($('completed'))$('completed').textContent=done;if($('pending'))$('pending').textContent=total-done;if($('progress'))$('progress').textContent=p+'%';if($('aProgress'))$('aProgress').textContent=p+'%';if($('todayBadge'))$('todayBadge').textContent=p+'%';if($('bar'))$('bar').style.width=p+'%';if($('miniDone'))$('miniDone').textContent=done;if($('miniPending'))$('miniPending').textContent=total-done;if($('focus'))$('focus').textContent=Math.min(100,p)+' / 100'}function escapeHtml(s){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function openDialog(){$('taskDialog').showModal();$('taskInput').focus()}function addTask(e){e.preventDefault();let title=$('taskInput').value.trim();if(title){tasks.unshift({id:Date.now(),title,priority:$('priority').value,done:false});$('taskInput').value='';$('taskDialog').close();save()}}document.addEventListener('click',e=>{let id=e.target.dataset.id||e.target.dataset.del;if(id){let t=tasks.find(x=>x.id==id);if(e.target.dataset.id)t.done=!t.done;else tasks=tasks.filter(x=>x.id!=id);save()}if(e.target.closest('.nav')){document.querySelectorAll('.nav').forEach(n=>n.classList.remove('active'));e.target.closest('.nav').classList.add('active');document.querySelectorAll('.section').forEach(s=>s.classList.remove('active'));$(e.target.closest('.nav').dataset.section).classList.add('active');}});$('addBtn')?.addEventListener('click',openDialog);$('addBtn2')?.addEventListener('click',openDialog);$('addTop')?.addEventListener('click',openDialog);$('taskForm')?.addEventListener('submit',addTask);['search','search2','filter','filter2'].forEach(id=>$(id)?.addEventListener('input',render));$('themeBtn')?.addEventListener('click',()=>{document.body.classList.toggle('dark');localStorage.setItem('dark',document.body.classList.contains('dark'));});$('darkSwitch')?.addEventListener('click',e=>{document.body.classList.toggle('dark');let on=document.body.classList.contains('dark');e.target.textContent=on?'ON':'OFF';e.target.classList.toggle('on',on)});$('redSwitch')?.addEventListener('click',e=>{e.target.classList.toggle('on');e.target.textContent=e.target.classList.contains('on')?'ON':'OFF'});$('clearBtn')?.addEventListener('click',()=>{if(confirm('Clear all tasks?')){tasks=[];save()}});if(localStorage.getItem('dark')==='true')document.body.classList.add('dark');render();